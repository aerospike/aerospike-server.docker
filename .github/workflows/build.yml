name: Docker Build
permissions:
  contents: read
on:
  push:
    branches: [main, master]
  pull_request:

jobs:
  generate-check:
    name: Generate (no diff)
    runs-on: ubuntu-24.04
    steps:
      - name: Harden the runner (Audit all outbound calls)
        uses: step-security/harden-runner@df199fb7be9f65074067a9eb93f12bb4c5547cf2 # v2.13.3
        with:
          egress-policy: audit
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Generate Dockerfiles for all support releases
        run: ./docker-build.sh -g 
      - name: Should not change anything
        run: '[ -z "$(git diff --stat)" ] || (git diff --stat; exit 1)'

  build-and-test:
    name: Build and test
    runs-on: ubuntu-24.04
    steps:
      - name: Harden the runner (Audit all outbound calls)
        uses: step-security/harden-runner@df199fb7be9f65074067a9eb93f12bb4c5547cf2 # v2.13.3
        with:
          egress-policy: audit
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - uses: actions/checkout@v4
      - name: Build
        run: ./docker-build.sh -t
      - name: Test
        run: ./test.sh -a amd64 -a arm64
